package command;

/**
 *
 * this is the interface for all our Job (Command OBjects) objects
 *
 * */
public interface Job {

    // only has one job method to implement and thats run!
    public void run();
}
